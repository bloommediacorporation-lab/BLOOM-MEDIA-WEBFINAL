{
  "name": "bloom-media-monorepo",
  "private": true,
  "scripts": {
    "build": "npm run vercel-build",
    "vercel-build": "node -e \"const cp=require('child_process'); const fs=require('fs'); const path=require('path'); const isVercel=!!process.env.VERCEL; const install=isVercel?'ci':'install'; cp.execSync('npm --prefix sveltekit_app ' + install + ' --no-audit --no-fund',{stdio:'inherit'}); cp.execSync('npm --prefix sveltekit_app run build',{stdio:'inherit'}); const src=path.join(process.cwd(),'sveltekit_app','.vercel','output'); const dst=path.join(process.cwd(),'.vercel','output'); fs.rmSync(dst,{recursive:true,force:true}); fs.mkdirSync(path.dirname(dst),{recursive:true}); fs.cpSync(src,dst,{recursive:true,force:true,dereference:true});\"",
    "convex": "convex dev"
  },
  "engines": {
    "node": "20.x"
  },
  "dependencies": {
    "@dimforge/rapier3d-compat": "^0.19.3",
    "@theatre/core": "^0.7.2",
    "@theatre/studio": "^0.7.2",
    "@threlte/core": "^8.3.1",
    "@threlte/extras": "^9.7.1",
    "@threlte/rapier": "^3.2.0",
    "@threlte/theatre": "^3.1.2",
    "@vercel/speed-insights": "^1.3.1",
    "convex": "^1.31.4",
    "convex-svelte": "^0.0.12",
    "gsap": "^3.14.2",
    "rollup": "^4.55.1",
    "vite": "^7.3.1"
  }
}
